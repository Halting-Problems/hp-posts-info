validation_summary:
  candidate: "lucide-proxy-npm-browser-ddos-campaign"
  local_results:
    - gate: "source binding"
      result: "pass"
      evidence: "Captured JFrog HTML SHA-256, event_profile.json, and site-worker-plan.json all equal dac4e36c6c63cdfc9ce13b8db4f9d67287877b7ec5bb9516ca8b76681f557fcf."
    - gate: "package/version extraction"
      result: "pass"
      evidence: "148 table rows, 148 unique package names, and 398 exact name/version pairs."
    - gate: "modular plan structure"
      command: "python skills/site-worker/scripts/validate_site_worker_plan.py /home/sam/hp-posts-info/lucide-proxy-npm-browser-ddos-campaign/site-worker-plan.json"
      result: "pass"
      evidence: "26-node plan; 23 completed local nodes, one skipped production import, and two blocked production gates."
    - gate: "focused incident script tests"
      command: "/home/sam/hp-posts-info/.venv/bin/pytest -q lucide-proxy-npm-browser-ddos-campaign/tests"
      result: "pass"
      evidence: "6 passed; clean, alert, and collection-failure paths for both collectors."
    - gate: "candidate-scoped authoring contracts"
      command: "/home/sam/hp-posts-info/.venv/bin/pytest -q tests/test_iocs.py tests/test_manifest.py tests/test_script.py -k lucide-proxy-npm-browser-ddos-campaign"
      result: "pass"
      evidence: "50 passed, 4906 deselected."
    - gate: "selected-slug Postgres importer dry-run"
      command: "pnpm exec tsx scripts/import-posts-info-to-postgres.ts --posts-info-dir /home/sam/hp-posts-info --slug lucide-proxy-npm-browser-ddos-campaign --dry-run"
      result: "pass"
      evidence: "validated: lucide-proxy-npm-browser-ddos-campaign"
    - gate: "agency-agent review contracts"
      result: "pass_with_warnings"
      evidence: "Five required review YAMLs contain all contract fields; each lane returned pass_with_warnings and no blocking findings."
    - gate: "run-ledger contract"
      result: "pass"
      evidence: "Required keys present, 26 task results accounted for, nextjs-postgres target fixed, decision needs_review."
  repository_pipeline_evidence_provided_by_parent:
    typescript_orchestration_tests: "81 passed"
    python_skill_tests: "46 passed, 4 failed"
    python_failure_classification: "Pre-existing stale site-refresh-orchestrator tests call removed classify_candidate/make_work_orders APIs after the builder became a deprecated TypeScript wrapper; not candidate-specific and not modified here."
  intentionally_blocked:
    - "Production Postgres import was not run."
    - "Production threat page, feed, and search verification were not run."
    - "Artifact-mode plan validation cannot pass because production-blocking nodes are intentionally incomplete."
    - "The validator environment also lacks the optional jsonschema Python dependency, so artifact schema checks fail closed; no package was installed to work around this."
  worktree_safety:
    website_repository: "clean after the local run"
    research_repository: "only the new candidate directory was added by this task; unrelated dirty files were preserved"
